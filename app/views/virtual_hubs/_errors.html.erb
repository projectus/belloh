<% if vhub.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(vhub.errors.count, "error") %> prohibited this virtual hub from being saved:</h2>
    <ul>
    <% vhub.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>